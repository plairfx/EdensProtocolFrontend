# syntax = docker/dockerfile:1.6
FROM ghcr.io/joshstevens19/rindexer-bundled:latest

WORKDIR /app

# Copy from dwindexer folder to Realindexer subfolder  
COPY dwindexer/Realindexer/ /app/

ARG PORT="3001"
ENV PORT=${PORT}
EXPOSE ${PORT}

ENTRYPOINT ["/app/rindexer"]
CMD ["start", "-p", "/app", "all"]